generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model portfolio {
  id    BigInt  @id @default(autoincrement())
  title String?
  link  String?
  desc  String?
  img   String?
  color String?
}
